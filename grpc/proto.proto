syntax = "proto3";
option go_package = "AquaWallah/grpc";

message Request {
    int64 time = 1;
}

message Reply {
    string message = 1;
}

message Empty {}

service AquaWallah {
    rpc SendRequest(Request) returns (Empty){}
    rpc SendReply(Reply) returns (Empty){}
}